<?xml version="1.0" encoding="utf-8" ?>

<UserControl

    x:Class="HailMary.Views.DavinciBatchRenderPanel"

    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"

    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

    xmlns:viewModels="using:HailMary.ViewModels"

    HorizontalAlignment="Left"
    VerticalAlignment="Top"
    mc:Ignorable="d" xmlns:loc="using:HailMary.Services">



    <ScrollViewer HorizontalContentAlignment="Left" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">

        <StackPanel Style="{StaticResource ToolShellContentPanel}" Spacing="10">

            <TextBlock Text="{x:Bind ViewModel.Description, Mode=OneWay}" TextWrapping="WrapWholeWords" />

            <TextBlock Style="{StaticResource CaptionTextBlockStyle}" Text="{x:Bind ViewModel.Status, Mode=OneWay}" />



            <Border Style="{StaticResource ToolShellCard}">

                <StackPanel Spacing="8">

                    <TextBlock Style="{StaticResource ToolShellSectionHeader}" loc:Loc.Key="common.queue" />

                    <Border

                        x:Name="DropZone"

                        Padding="12"

                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"

                        BorderThickness="2"

                        CornerRadius="4"

                        IsHitTestVisible="{x:Bind ViewModel.CanEditQueue, Mode=OneWay}">

                        <TextBlock HorizontalAlignment="Center" Opacity="0.7" loc:Loc.Key="davincibatch.dropHint" />

                    </Border>

                    <ListView

                        x:Name="QueueList"

                        AllowDrop="{x:Bind ViewModel.CanEditQueue, Mode=OneWay}"

                        MaxHeight="180"

                        ItemsSource="{x:Bind ViewModel.QueueRows, Mode=OneWay}"

                        SelectionChanged="QueueList_OnSelectionChanged">

                        <ListView.ItemTemplate>

                            <DataTemplate x:DataType="viewModels:DavinciBatchRenderQueueRow">

                                <Grid ColumnSpacing="8">

                                    <Grid.ColumnDefinitions>

                                        <ColumnDefinition Width="*" />

                                        <ColumnDefinition Width="Auto" />

                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" FontFamily="Consolas" Text="{x:Bind FileName}" TextTrimming="CharacterEllipsis" />

                                    <TextBlock Grid.Column="1" Opacity="0.7" Text="{x:Bind Status}" />

                                </Grid>

                            </DataTemplate>

                        </ListView.ItemTemplate>

                    </ListView>

                    <StackPanel Orientation="Horizontal" Spacing="8">

                        <Button Command="{x:Bind ViewModel.PickVideosCommand}" loc:Loc.Key="common.pickFiles" IsEnabled="{x:Bind ViewModel.CanEditQueue, Mode=OneWay}" />

                        <Button Command="{x:Bind ViewModel.PickFolderCommand}" loc:Loc.Key="common.folder" IsEnabled="{x:Bind ViewModel.CanEditQueue, Mode=OneWay}" />

                        <Button Command="{x:Bind ViewModel.RemoveSelectedCommand}" loc:Loc.Key="common.remove" IsEnabled="{x:Bind ViewModel.CanEditQueue, Mode=OneWay}" />

                        <Button Command="{x:Bind ViewModel.ClearQueueCommand}" loc:Loc.Key="common.clear" IsEnabled="{x:Bind ViewModel.CanEditQueue, Mode=OneWay}" />

                    </StackPanel>

                </StackPanel>

            </Border>



            <Border Style="{StaticResource ToolShellCard}">

                <StackPanel Spacing="8">

                    <TextBlock Style="{StaticResource ToolShellSectionHeader}" loc:Loc.Key="common.export" />

                    <Grid ColumnSpacing="8" RowSpacing="8">

                        <Grid.ColumnDefinitions>

                            <ColumnDefinition Width="120" />

                            <ColumnDefinition Width="*" />

                            <ColumnDefinition Width="Auto" />

                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>

                            <RowDefinition />

                            <RowDefinition />

                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" loc:Loc.Key="common.preset" />

                        <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Text="{x:Bind ViewModel.DavinciPreset, Mode=TwoWay}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" loc:Loc.Key="bitrate.outputLabel" />

                        <TextBox Grid.Row="1" Grid.Column="1" Text="{x:Bind ViewModel.OutputDir, Mode=TwoWay}" />

                        <Button Grid.Row="1" Grid.Column="2" Command="{x:Bind ViewModel.PickOutputDirCommand}" Content="…" />

                    </Grid>

                    <CheckBox loc:Loc.Key="davincibatch.safeOutput" IsChecked="{x:Bind ViewModel.SafeOutput, Mode=TwoWay}" />

                    <TextBlock

                        Style="{StaticResource CaptionTextBlockStyle}"

                        loc:Loc.Key="davincibatch.resolvePathHint"

                        TextWrapping="WrapWholeWords" />

                    <StackPanel Orientation="Horizontal" Spacing="8">

                        <Button Command="{x:Bind ViewModel.StopBatchCommand}" loc:Loc.Key="common.stop" IsEnabled="{x:Bind ViewModel.CanStop, Mode=OneWay}" />

                        <Button Command="{x:Bind ViewModel.SaveSettingsCommand}" loc:Loc.Key="common.save" />

                    </StackPanel>

                </StackPanel>

            </Border>

        </StackPanel>

    </ScrollViewer>

</UserControl>


