<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="HailMary.Views.Controls.AspectFitImagePresenter"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    HorizontalAlignment="Left"
    VerticalAlignment="Top"
    mc:Ignorable="d"
    SizeChanged="Root_OnSizeChanged">

    <Grid x:Name="LayoutHost" HorizontalAlignment="Left" VerticalAlignment="Top">
        <Border
            x:Name="FrameChrome"
            Background="{ThemeResource HmPreviewChromeBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="4">
            <Image
                x:Name="PreviewImage"
                Stretch="Fill"
                ImageOpened="PreviewImage_OnImageOpened" />
        </Border>
    </Grid>
</UserControl>
