# Python packages for Hail Mary bridge jobs (optional tools).
# Install via setup_python.bat into the local .venv folder.

opencv-python
numpy
Pillow
watchdog

# Optional — only needed for Audio Cleaner with DeepFilterNet:
# deepfilternet
